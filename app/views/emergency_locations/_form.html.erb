<%= form_with(model: emergency_location, local: true) do |form| %>
  <% if emergency_location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(emergency_location.errors.count, "error") %> prohibited this emergency_location from being saved:</h2>

      <ul>
      <% emergency_location.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
