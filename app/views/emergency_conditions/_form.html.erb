<%= form_with(model: emergency_condition, local: true) do |form| %>
  <% if emergency_condition.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(emergency_condition.errors.count, "error") %> prohibited this emergency_condition from being saved:</h2>

      <ul>
      <% emergency_condition.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
